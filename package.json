{
  "name": "irc-taxi-booking-site",
  "version": "1.0.0",
  "description": "IRC Taxi client website with a WhatsApp-only booking form — no database.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
